# ~/.zshrc.local
# Machine-specific overrides go here. This file stays out of version control.
# Add tokens, private paths, or host-specific tweaks below.

# Example:
# export PATH="$HOME/.local/bin:$PATH"
